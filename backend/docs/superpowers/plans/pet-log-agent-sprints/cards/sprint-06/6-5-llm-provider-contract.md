# Card 6-5: LLM provider contract test

**목표:** 실제 LLM provider가 기존 interface를 충족하는지 검증하는 계약 테스트를 만든다.

**Files:**
- Test: `tests/test_llm_provider_contract.py`

**완료 기준:**
- [x] `RecordStructurerInterface` 계약 테스트를 만든다.
- [x] `CareAnswerProviderInterface` 계약 테스트를 만든다.
- [x] `PetPersonaResponderInterface` 계약 테스트를 만든다.
